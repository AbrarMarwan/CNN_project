# تصنيف رسومات الأطفال نفسيًا باستخدام الذكاء الاصطناعي

## فكرة المشروع
تصنيف رسومات الأطفال إلى فئات نفسية (سعيد، حزين، خايف...) باستخدام نموذج ذكاء اصطناعي مدرّب على بيانات مفتوحة.

##تقسيم المجلدات
drawingEmotionClassfication-main/
│
├── 📁 dataset/                         ← بيانات التدريب والاختبار
│   ├── 📁 train/                       ← صور التدريب (مقسمة حسب المشاعر)
│   │   ├── Angry/
│   │   ├── Fear/
│   │   ├── Happy/
│   │   └── Sad/
│   └── 📁 test/                        ← صور الاختبار (نفس التقسيم)
│       ├── Angry/
│       ├── Fear/
│       ├── Happy/
│       └── Sad/
│
├── 📁 src/                             ← الكود الأساسي للمشروع
│   ├── 📁 cnn/                         ← كود التدريب وحفظ النموذج
│   │   └── MobileNetV2_Model.ipynb                     ← كود تدريب النموذج (MobileNetV2 أو CNN)
│   │
│   ├── 📁 api/                         ← واجهة Flask API
│   │   ├── app.py                     ← كود Flask لتحليل الصور
│   │   └── mobileNetV2_model.h5       ← النموذج المدرب المحفوظ
│   │
│   └── 📁 streamlit/                   ← واجهة Streamlit للمستخدم
│       └── app.py                     ← كود Streamlit لرفع الصورة وعرض النتيجة
│
├── 📄 README.md                        ← وصف المشروع وطريقة الاستخدام
├── 📄 requirements.txt                 ← قائمة المكتبات المطلوبة (Flask, TensorFlow, Streamlit...)


## محتوى المشروع
- `MobileNetV2_Model.ipynb` : كود تدريب النموذج ومعالجة البيانات
- `mobileNetV2_Model.h5` : النموذج المدرّب
- `api/app.py` : REST API لاستقبال صورة وإرجاع التصنيف
- `streamlit/app.py` : واجهة المستخدم (رفع صورة ومعرفة النتيجة)
- `requirements.txt` : المكتبات المطلوبة
- `README.md` : هذا الملف

## خطوات التشغيل
1. تثبيت المكتبات:  
   ```
   pip install -r requirements.txt
   ```
2. تدريب النموذج (اختياري إذا أردت إعادة التدريب):  
   ```
   cnn  MobileNetV2_Nodel.ipynb
   ```
3. تشغيل الـ API:  
   ```
   api app.py
   ```
4. تشغيل الواجهة:  
   ```
   streamlit run app.py
   ```

## النتائج
- الدقة على بيانات الاختبار: 93.43%
- الافضل الدقة 90.21 عشان بقية المقاييس ارتفعت معاه 

## تحسينات مستقبلية
- دعم المزيد من المشاعر.
- تحسين الواجهة لتدعم اللغة العربية.

---

## مصدر البيانات
https://universe.roboflow.com/anamel/anamelclassification/dataset/1
https://www.kaggle.com/datasets/vishmiperera/children-drawings